# Internal function for placebo checks

Internal function for placebo checks

## Usage

``` r
.run_placebo_checks(
  y,
  X_full,
  n_pre,
  kernel_type,
  period,
  time_col,
  scale,
  interval_type,
  alpha,
  optimize,
  mixed_data,
  cat_columns,
  placebo_periods,
  verbose
)
```
